import io;
import default string;


fn main() -> i32{
    std::string strcpp = "Ini C++ String";
    var rypstr: str = "Ini ryp string";

    io::println(strcpp);
    std::cout << rypstr << std::endl;

    return 0;
}